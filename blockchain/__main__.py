# from blockchain import Blockchain, db
#
# db.truncate() # clears the list everytime the program is ran so the same things dont get added for testing
# blockchain = Blockchain()
# t1 = blockchain.new_transaction("movie", "file_id", "horror",".mp4", '200gb')
# blockchain.new_block(1)
# t2 = blockchain.new_transaction("song", "file_id", "pop", ".mp3", '19mb')
# blockchain.new_block(2)
# t3 = blockchain.new_transaction("book", "file_id", "comedy", ".epub", '17mb')
# blockchain.new_block(3)
#
#
#
# # print("bChain Ledger: ", blockchain.chain)
# print("bChain Ledger: ", db.all())

# t4 = blockchain.new_transaction("folder", "goverment", '1gb')
# t5 = blockchain.new_transaction("files", "top-secret", '5mb')
# t6 = blockchain.new_transaction("documents", "shhhhh", '5mb')
# blockchain.new_block(6789)
